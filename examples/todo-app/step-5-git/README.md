# Step 5 — Git

Copy files from `step-4-persist/` into this folder, then:

```bash
git init
git add .
git commit -m "Add todo app with localStorage persistence"
```

Run `/devlearn-git` while committing to learn staging, commits, and messages.

Optional: push and open a PR with `/devlearn-git` Workflow C.
